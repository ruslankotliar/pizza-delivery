import { ProfilePageComponent } from '../components/index';

const page = {
  element: ProfilePageComponent,
  path: 'USER_PROFILE',
  protectedRoute: true,
  key: 'user_profile',
};

export default page;
