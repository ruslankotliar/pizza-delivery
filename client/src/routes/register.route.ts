import { RegistrationPageComponent } from "../components/index";

const page = {
  element: RegistrationPageComponent,
  path: 'USER_REGISTER',
  protectedRoute: false,
  key: 'register',
};

export default page;
